-- show mysql version
mysql --version

-- login to MySQL Shell root user - password
mysql -u root -p

-- execute a file that contains a list of commands
source schema.sql

-- Check if Database in Use ---
SELECT DATABASE();

-- gtfo
quit